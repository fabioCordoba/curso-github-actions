name: Hola Mundo Workflow

on:
  push:
    paths:
      - "script-hola-mundo.py"   # Se ejecuta solo si cambia el script
  workflow_dispatch: # Permite correrlo manualmente desde GitHub UI

jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout del código
        uses: actions/checkout@v4

      - name: 🐍 Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: ▶️ Ejecutar script Hola Mundo
        run: python script-hola-mundo.py
